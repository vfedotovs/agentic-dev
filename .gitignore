# Local secrets — never commit
agentic-dev.env
*.env
!agentic/host/agentic-dev.env.example

# Python
__pycache__/
*.py[cod]

# Runtime state (created on the host / in containers)
runs/
*.log

# Local scratch notes
todo.md
